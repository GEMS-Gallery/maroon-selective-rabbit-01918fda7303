service : {
  getGameState: () ->
   (vec record {
          commanderDamage: vec int;
          lifeTotal: int;
        }) query;
  resetGame: () -> ();
  updateCommanderDamage: (nat, nat, int) -> ();
  updateLifeTotal: (nat, int) -> ();
}
